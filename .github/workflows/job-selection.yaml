name: JOB Selection

on:
  workflow_dispatch:
    inputs:
      job_name:
        description: Name of the job
        required: true
        default: none
        type: choice
        options:
          - job-one
          - job-two

jobs:
  job-one:
    if: inputs.environment == 'job-one'
    runs-on: self-hosted
    steps:
      - name: Print the output
        run: |
          echo "Job ONE"

  job-two:
    if: inputs.environment == 'job-two'
    runs-on: self-hosted
    steps:
      - name: Print the output
        run: |
          echo "Job TWO"
