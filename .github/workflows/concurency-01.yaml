name: Concurency 01

on:
  workflow_dispatch:

concurrency:
  group: deploy-application

jobs:
  display_context:
    runs-on: self-hosted
    steps:
          - name: Display Contexts
            run: | 
              sleep 60
              echo I finished at $(date +%s)
